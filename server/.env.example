# Port the API listens on.
PORT=3000

# Where the JSON score store is written.
DATA_FILE=data/scores.json

# Allowed browser origin(s). "*" is fine for local development; set a real
# origin before deploying a Flutter-web build against this API.
CORS_ORIGIN=*
